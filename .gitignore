# notes (local scratch; keep backlog tracked)
.notes/*
!.notes/TRACKED_BACKLOG.md
!.notes/FE_MVP_SPEC.md
!.notes/FE_TRACKED_BACKLOG.md

# dependencies
node_modules/

# build artifacts
dist/

# runtime data
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
!fixtures/demo-workspace.sqlite

# temp files
tmp/
temp/
*.tmp

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# test artifacts
coverage/
.nyc_output/
allure-results/
allure-report/
artifacts/
.stryker-tmp/
# Stryker default HTML at repo root only (do not ignore docs/reports/)
/reports/

# environment
.env
.env.local
.env.*.local

# Cursor / Penpot MCP scratch (no secrets in tracked mcp.json; token lives in .env.local)
.cursor/phase-a-exec-body.js
.cursor/mcp-execute-payload.json

# editor/OS
.DS_Store
Thumbs.db
.idea/
.vscode/